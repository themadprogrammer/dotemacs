;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Lisp fun

; Setup Lisp interpreters
(setq inferior-lisp-program "/usr/bin/sbcl")
;(setq inferior-lisp-program "/usr/bin/clisp")
;(setq inferior-lisp-program "/usr/bin/sbcl") 

(provide 'init-lisp)
